
{{--<span class="badge {{array_sum(array_map("count", $product_list_requests)) > 0 ? 'badge-danger': 'badge-primary'}} ">
       &nbsp;  {{array_sum(array_map("count", $product_list_requests)) }}
</span>--}}
{{--<button type="button" class="btn btn-primary">
    Cooperation requests <span class="badge {{array_sum(array_map("count", $product_list_requests)) > 0 ? 'btn-danger': 'btn-primary'}}badge-light">{{array_sum(array_map("count", $product_list_requests)) }}</span>
</button>--}}
